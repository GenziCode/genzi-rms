// Legacy NotificationCenter component removed. This placeholder prevents module resolution errors
// for any stale imports that might still point to this path. The inbox experience now lives in
// `src/pages/NotificationsPage.tsx`.

export default function NotificationCenterPlaceholder() {
  return null;
}
