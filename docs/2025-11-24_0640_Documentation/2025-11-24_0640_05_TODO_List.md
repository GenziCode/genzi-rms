# 📋 TODO List – Generated on 2025-11-24 06:40

## ✅ Completed (as of now)
- Project Overview (01)
- Backend Status (02)
- Frontend Status (03)
- Implementation Plan (04)

## ❌ Pending Tasks (ordered by priority)

### 🔴 Critical Front‑end UI
1. **Invoice Management UI**
   - Build `InvoicesPage` (list view).
   - Create `InvoiceFormModal` (create / edit).
   - Integrate invoice templates (modern, classic, minimal, professional, thermal).
   - Add PDF export (pdfkit) and print button.
   - Add email/SMS send dialogs (hook to backend).
2. **Notification Center UI**
   - Navbar dropdown component.
   - `NotificationCenterPage` (list, pagination).
   - Mark‑as‑read & bulk‑read actions.
   - Preferences page (email/SMS toggles).
3. **Payment UI**
   - `PaymentPage` (history table).
   - Stripe checkout form (client‑side tokenization).
   - Refund UI (modal).
   - Confirmation flow & success toast.
4. **Inventory – Stock Transfer UI**
   - Add **Transfer Stock** button on `InventoryPage`.
   - `StockTransferModal` (source store, target store, quantity).
   - Hook to backend `/api/inventory/transfer`.

### 🟠 High‑Priority Front‑end Enhancements
5. **Audit‑Log Viewer**
   - `AuditLogsPage` with timeline view.
   - Filters: user, action type, entity, date range.
   - CSV export button.
   - Pagination / infinite scroll.
6. **Webhook Configuration UI**
   - `WebhooksPage` (list, create, edit).
   - Test webhook button (calls backend test endpoint).
   - Delivery logs viewer (status, timestamps).
7. **User Profile Page**
   - Profile view/edit (name, email).
   - Change password flow.
   - Avatar upload (uses file‑upload endpoint).
   - Preferences (theme, language).

### 🟡 Medium‑Priority Front‑end Polish
8. **Polish Existing Pages**
   - Add loading spinners, error toasts, accessibility tweaks.
   - Run UI regression tests (Cypress).

### ⚪ Optional Backend Public API & SDK
9. **API‑Key Management**
   - Model, CRUD routes, admin protection.
10. **Rate‑Limiting per Key**
    - Middleware reading usage from Redis, configurable quotas.
11. **Swagger/OpenAPI Docs**
    - Install `swagger-ui-express`, generate `swagger.json`, serve at `/api/docs`.
12. **SDK Generation Scripts**
    - CLI script (`scripts/generate-sdk.js`) to output JS & Python client libraries.

---

**How to use:**
- Open each markdown file in the `2025-11-24_0640_Documentation` folder.
- Mark tasks as done by adding a ✅ in the corresponding line.
- Follow the day‑by‑day tables in the Implementation Plan (04) for sprint planning.
